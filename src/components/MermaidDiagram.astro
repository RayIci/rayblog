---
import { Mermaid } from "@/components/Mermaid";

interface Props {
  code: string;
  width?: string;
  maxWidth?: string;
}

const { code, width, maxWidth } = Astro.props;
---

<Mermaid client:load code={code} width={width} maxWidth={maxWidth} />
